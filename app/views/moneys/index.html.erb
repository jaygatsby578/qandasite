<h1>Moneys#index</h1>
<p>※現在、購入した記録の一覧です。</p>
<table class="table">
    <tr>
        <th>ID</th><th>日付</th><th>項目</th><th>金額</th><th>分類</th><th>コメント</th>
    </tr>
    <% @moneys.each do |obj| %>
    <tr>
        <td width="35px"><%= obj.id %></td>
        <td><%= obj.date %></td>
        <td><%= obj.item %></td>
        <td><%= obj.price %></td>
        <td><%= obj.category %></td>
        <td><%= obj.memo %></td>
        <td><a href="/moneys/edit/<%= obj.id %>">編集</td>
        <td><a href="javascript:delData(
            <%= (obj.id.to_s + ",'".to_s + obj.item.to_s).html_safe %>')">削除</a></td>
    </tr>
    <% end %>
</table>
<p><a href="/moneys/add">※新しい項目を入力 &gt;&gt;</a></p>

<script>
function delData(id,item){
    if (confirm('「' + item + '」のデータを削除しますか？')){
        document.location = "/moneys/delete/" + id;
    }
}
</script>